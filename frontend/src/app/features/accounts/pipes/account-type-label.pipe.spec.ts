import { AccountTypeLabelPipe } from './account-type-label.pipe';

describe('AccountTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
