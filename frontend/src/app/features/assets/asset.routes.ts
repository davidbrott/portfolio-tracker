import { Routes } from "@angular/router";
import { AssetComponent } from "./components/container/asset/asset.component";

export const ASSET_ROUTES: Routes = [
    {
        path: '',
        component: AssetComponent
    }
]